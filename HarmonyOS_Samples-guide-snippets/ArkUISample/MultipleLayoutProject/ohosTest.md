# ArkUI多种布局组件 测试用例归档

## 用例表

| 测试功能                                                                            | 预置条件       | 输入                   | 预期输出    | 是否自动 | 测试结果 |
|---------------------------------------------------------------------------------| -------------- |----------------------|---------|:-----| -------- |
| 测试RelativeContainer布局，RelativeContainer父组件为锚点，__container__代表父容器的组件标识（id）是否构建成功 | 设备正常运行   | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，以兄弟元素为锚点是否构建成功                                            | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，子组件锚点可以任意选择，但需注意不要相互依赖是否构建成功                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，子组件位置偏移是否构建成功                                             | 设备正常运行   | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，多种组件的对齐布局是否构建成功                                           | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，组件尺寸是否构建成功                                                | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，多个组件形成链是否构建成功                                             | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，使用辅助线辅助定位子组件是否构建成功                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试RelativeContainer布局，多个组件的屏障是否构建成功                                             | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Stack布局，包含各种子元素是否构建成功                                                         | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Stack布局，对齐方式是否构建成功                                                            | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Stack布局，没有Z序控制是否构建成功                                                          | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Stack布局，Z序控制是否构建成功                                                            | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Stack布局，多种子组件场景示例是否构建成功                                                       | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，Column容器内排列方向上的间距是否构建成功                                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，Row容器内排列方向上的间距是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，元素在垂直方向首端对齐，第一个元素与行首对齐，同时后续的元素与前一个对齐是否构建成功                           | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，元素在垂直方向中心对齐，第一个元素与行首的距离与最后一个元素与行尾距离相同是否构建成功                          | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，元素在垂直方向尾部对齐，最后一个元素与行尾对齐，其余元素与后一个对齐是否构建成功                             | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，垂直方向均匀分配元素，相邻元素之间距离相同是否构建成功                                          | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，垂直方向均匀分配元素，相邻元素之间距离相同是否构建成功                                          | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，垂直方向均匀分配元素，相邻元素之间的距离、第一个元素与行首的间距、最后一个元素到行尾的间距都完全一样是否构建成功             | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，元素在水平方向首端对齐，第一个元素与行首对齐，同时后续的元素与前一个对齐是否构建成功                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，元素在水平方向中心对齐，第一个元素与行首的距离与最后一个元素与行尾距离相同是否构建成功                             | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，元素在水平方向尾部对齐是否构建成功                                                       | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，水平方向均匀分配元素，相邻元素之间距离相同是否构建成功                                             | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，水平方向均匀分配元素，相邻元素之间距离相同是否构建成功                                             | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，水平方向均匀分配元素，相邻元素之间的距离、第一个元素与行首的间距、最后一个元素到行尾的间距都完全一样是否构建成功                | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，子元素在水平方向左对齐是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，子元素在水平方向居中对齐是否构建成功                                                   | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Column布局，子元素在水平方向右对齐是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，子元素在垂直方向顶部对齐是否构建成功                                                      | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，子元素在垂直方向居中对齐是否构建成功                                                      | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Row布局，子元素在垂直方向底部对齐是否构建成功                                                      | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试线性布局下自适应拉伸是否构建成功                                                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试线性布局下自适应缩放是否构建成功                                                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试线性布局下，自适应延伸水平方向是否构建成功                                                         | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试线性布局下，自适应延伸垂直方向是否构建成功                                                         | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,主轴为水平方向，子元素从起始端沿着水平方向开始排布是否构建成功                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,主轴为水平方向，子元素从终点端沿着FlexDirection.Row相反的方向开始排布是否构建成功                      | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,主轴为垂直方向，子元素从起始端沿着垂直方向开始排布是否构建成功                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,主轴为垂直方向，子元素从终点端沿着FlexDirection.Column相反的方向开始排布是否构建成功                   | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,不换行,如果子元素的宽度总和大于父元素的宽度，则子元素会被压缩宽度是否构建成功                                | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,换行，每一行子元素按照主轴方向排列是否构建成功                                                | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,换行，每一行子元素按照主轴反方向排列是否构建成功                                               | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,元素在主轴方向起始端对齐是否构建成功                                                     | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素在主轴方向居中对齐是否构建成功                                                     | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素在主轴方向终点端对齐是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,Flex主轴方向均匀分配弹性元素，相邻子元素之间距离相同是否构建成功                                     | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,Flex主轴方向元素等间距布局是否构建成功                                                  | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,使用Flex容器中默认配置是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,交叉轴方向首部对齐是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,交叉轴方向居中对齐是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,交叉轴方向底部对齐是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,交叉轴方向拉伸填充，在未设置尺寸时，拉伸到容器尺寸是否构建成功                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  |是       | Pass     |
| 测试Flex布局,交叉轴方向文本基线对齐是否构建成功                                                      | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素设置交叉轴对齐是否构建成功                                                       | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素各行与交叉轴起点对齐是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素各行在交叉轴方向居中对齐是否构建成功                                                  | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素各行与交叉轴终点对齐是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素各行与交叉轴两端对齐，各行间垂直间距平均分布是否构建成功                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素各行间距相等，是元素首尾行与交叉轴两端距离的两倍是否构建成功                                      | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,子元素各行间距，子元素首尾行与交叉轴两端距离都相等是否构建成功                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,设置子元素在父容器主轴方向上的基准尺寸是否构建成功                                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,设置父容器的剩余空间分配给此属性所在组件的比例是否构建成功                                          | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,当父容器空间不足时，子元素的压缩比例是否构建成功                                               | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试Flex布局,多种子组件场景示例是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，通过reference设置断点切换参考物是否构建成功                                               | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，通过columns设置栅格布局的总列数为4是否构建成功                                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，通过columns设置栅格布局的总列数为8是否构建成功                                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，为GridRowColumnOption时是否构建成功                              | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，子组件默认从左往右排列是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，子组件从右往左排列是否构建成功                                                          | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当gutter类型为number时是否构建成功                           | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当gutter类型为GutterOption时是否构建成功        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，子组件GridCol设置span是否构建成功                                                   | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，子组件GridCol设置offset是否构建成功                                     | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，子组件GridCol设置order是否构建成功                                                  | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当span类型为number时是否构建成功                                                    | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当span类型为GridColColumnOption时是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当offset类型为number时是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当offset类型为GridColColumnOption时是否构建成功                                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当order类型为number时是否构建成功                                        | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，当order类型为GridColColumnOption时是否构建成功                                                      | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |
| 测试栅格布局，栅格组件的嵌套使用是否构建成功                                                       | 设备正常运行 | 进入各个示例页面，push页面，然后返回 | 页面跳转正常  | 是    | Pass     |